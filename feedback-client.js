import{initializeApp,getApps}from'https://www.gstatic.com/firebasejs/10.12.5/firebase-app.js';import{getAuth,onAuthStateChanged}from'https://www.gstatic.com/firebasejs/10.12.5/firebase-auth.js';import{getFirestore,doc,updateDoc,arrayUnion,serverTimestamp}from'https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js';const app=getApps()[0]||initializeApp(window.MITKADMIM_FIREBASE_CONFIG),auth=getAuth(app),db=getFirestore(app);let user=null;onAuthStateChanged(auth,u=>user=u);window.MitkadmimFeedback={send:async data=>{if(!user)throw new Error('not-authenticated');const item={id:(crypto.randomUUID?.()||Date.now().toString(36)),type:String(data.type||'feedback'),body:String(data.body||'').slice(0,1200),screen:String(data.screen||''),profileName:String(data.profileName||''),createdAt:new Date().toISOString(),status:'new'};await updateDoc(doc(db,'users',user.uid),{feedback:arrayUnion(item),feedbackUpdatedAt:serverTimestamp()});return item}};